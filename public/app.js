// public/app.js

// Functionality for creating new rooms and joining existing rooms

const createRoom = (roomName) => {
    // Logic for creating a new room
    console.log(`Room ${roomName} created`);
};

const joinRoom = (roomName) => {
    // Logic for joining existing room
    console.log(`Joined room ${roomName}`);
};

// Sample usage:
createRoom('room1');
joinRoom('room1');
